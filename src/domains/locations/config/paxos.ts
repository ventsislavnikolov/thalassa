import type { LocationConfig } from "../types";

const paxos: LocationConfig = {
  slug: "paxos",
  name: "Paxos",
  region: "Ionian Islands",
  country: "Greece",
  coordinates: {
    latitude: 39.2,
    longitude: 20.17,
  },
  timezone: "Europe/Athens",
  image: "/images/locations/paxos.webp",
};

export default paxos;
